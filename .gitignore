node_modules/
__pycache__/
*.pyc
.env
.env.*
*.log
.DS_Store
# runtime state (never committed)
prompt-log.jsonl
pending/
cooldown/
backstop.json
config.json

# Fable research scratch — experimental, may touch credentials; never commit
scratch/
RESEARCH-*.md

# context-hints runtime stores (seeded per user)
criteria.json
projects.json
HANDOFF.md

# launch/marketing working files — never public
brag-output*/
tasks/marketing-plan.md
tasks/launch-posts.md
